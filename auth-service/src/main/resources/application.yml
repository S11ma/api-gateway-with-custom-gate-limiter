server:
  port: 8083

spring:
  application:
    name: auth-service

# This secret MUST match the one in gateway-service/application.yml -
# both services sign/verify with the same HMAC key (symmetric signing).
# In a real deployment this would come from a config server or vault,
# never checked into git as plaintext.
jwt:
  secret: 8C+847MpsW3qHYF/6/7hrFPyxYwRuMzzbkoWVDUq1DE=
  expiration-ms: 3600000
